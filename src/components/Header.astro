---
import HeaderMenu from "@data/global/header-menu.json";
const wordmark = "/images/components/header/wordmark-circle-rotated-transparency-173x173px.png";
const currentPath = Astro.url.pathname;
---

<header class="bg-blue">
	<div class="container">
		<a href="/" class="wordmark-link">
			<img src={wordmark} alt="Word mark: Letters PM inside a circle." width="173" height="173" />
		</a>
		<ul>
			{
				HeaderMenu.map((menuItem) => (
					<li>
						<a
							class:list={[
								{
									"current-page": currentPath === menuItem.url,
								},
							]}
							href={menuItem.url}>
							{menuItem.text}
						</a>
					</li>
				))
			}
		</ul>
	</div>
</header>

<style lang="scss">
	@import "../styles/base/_variables.scss";

	.wordmark-link {
		margin-bottom: 1.5rem;

		@media screen and (min-width: $breakpoint-md) {
			margin-bottom: 0;
		}
	}

	header {
		box-shadow: 0 1rem 2rem 0.1rem rgba(black, 0.1);
		margin-bottom: 6rem;
	}

	div {
		align-items: center;
		display: flex;
		flex-direction: column;
		justify-content: space-between;
		padding-top: 2rem;
		padding-bottom: 2rem;

		@media screen and (min-width: $breakpoint-md) {
			flex-direction: row;
		}
	}

	img {
		width: 64px;
	}

	ul {
		list-style: none;
		margin: 0;
		padding-left: 0;
	}

	li {
		display: inline;
		margin-left: 0;
		font-size: 1.25rem;

		&:not(:first-of-type) {
			margin-left: 1.25rem;
		}
	}

	a {
		color: white;
		text-decoration: none;

		&:hover {
			color: white;
			text-decoration: underline;
		}
	}

	.current-page {
		font-weight: var(--fw-bold);
	}
</style>
